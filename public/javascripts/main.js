

console.log("here in main.js");
/* 
define('lib/angular', function () {
    if (angular) {
        return angular;
    }
    return {};
});

define([
  'angular'
], function(angular){

  // define our app as an angular module
  return angular.module("app", ['ngRoute', 'ui.bootstrap', 'ngGrid']);
});

define('AppController', function () {
    if (AppController) {
        return AppController;
    }
    return {};
});

define('lib/DocDomain', function () {
    if (DocDomain) {
        return DocDomain;
    }
    return {};
});

define('lib/SearchGrid', function () {
    if (SearchGrid) {
        return SearchGrid;
    }
    return {};
});
 
define('lib/SearchGridMap', function () {
    if (SearchGridMap) {
        return SearchGridMap;
    }
    return {};
});
 
define('lib/djConfig', function () {
    if (djConfig) {
        return djConfig;
    }
    return {};
});
 
define('lib/angular/angular-route', function () {
    if (djConfig) {
        return djConfig;
    }
    return {};
});
 
define('lib/Modal311', function () {
    if (Modal311) {
        return Modal311;
    }
    return {};
});

define('setup', function () {
    if (setup) {
        return setup;
    }
    return {};
});

define('init', function () {
    if (init) {
        return init;
    }
    return {};
});

  */
/* 
define('dojo', function () {
    if (dojo) {
        return dojo;
    }
    return {};
});


define('dojo/domReady', function () {
    if (ready) {
        return ready;
    }
    return {};
});

define('esri/arcgis/Portal', function () {
    if (esriarcgisportal) {
        return esriarcgisportal;
    }
    return {};
});
 */
